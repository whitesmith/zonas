import { StyleSheet } from 'react-native';

export const searchRowStyles = StyleSheet.create({
  containerStyle: {
    justifyContent: 'center',
    alignItems: 'center',
    flexDirection: 'row',
    padding: 10,
    borderColor: 'black',
    height: 50,
    borderWidth: 0.5
  }
});
