import { StyleSheet } from 'react-native';

export const searchBarStyles = StyleSheet.create({
  containerStyle: {
    padding: 10
  },
  inputStyle: {
    marginTop: 30,
    height: 50,
    borderWidth: 0.5,
    borderColor: 'black',
    padding: 5
  }
});
