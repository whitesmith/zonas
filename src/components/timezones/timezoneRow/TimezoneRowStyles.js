import { StyleSheet } from 'react-native';

export const timezoneRowStyles = StyleSheet.create({
  hourRowStyle: {
    flexDirection: 'row',
    marginBottom: 10,
    marginHorizontal: 5
  },
  hourSquareStyle: {
    justifyContent: 'center',
    alignItems: 'center',
    height: 50,
    borderColor: 'black'
  }
});
